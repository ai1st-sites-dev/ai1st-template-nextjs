# dependencies
# 🔴 没有尾斜杠是【故意】的（#1239 打磨批次 #28 条 9，来源 #1238）：带尾斜杠的规则只匹配【目录】，
# 而 git 把**符号链接**当文件 ⟹ `ln -s <共享树>/node_modules node_modules`（`AI_TEAM.md §体检…`
# 第 4 步教的、以及隔离 worktree 里起 vite 的标准做法）造出来的那一个**不被忽略**，`git add -A`
# 会把一条指向别人那棵树的符号链接暂存进交付。一次性 clone 里现测：符号链接 `git check-ignore -q`
# rc=1（没挡住）· 真目录 rc=0（挡住了）。去掉尾斜杠之后两种形态都挡得住，而这条规则唯一变宽的
# 地方是「一个名叫 node_modules 的普通文件」—— 那正是我们要挡的那一个。
# 📌 `ship-check-ignore-barrier.sh` 按【规则原文】分类，所以改这一行必须同一次改动里改它那张
# 非机密清单（`'node_modules'`）—— 那道守卫就是为了逼这件事存在的。
node_modules

# next.js
.next/
out/
# #1157（来源 #1152）—— `scripts/move-build-output.js:9` 每次构建都可能留下它：`backup` 那一支把
# 现有的 out/ 整个 rename 成 .out-backup/，只有 `restore` 跑完才搬回去 ⟹ 构建中途失败就留在盘上。
# 不忽略的后果不是"多个目录"：它长得跟「有人漏 stage 了一个交付物」一模一样，而 ship 前的
# stage-completeness / stale-index 那一族正要人逐条判那个（`ai-team/SHIPPING.md` 的规定动作）。
.out-backup/
# #1158（来源 #1157）—— 同一个脚本的**同一族**残留，同一行下面那一行造它（`:10`）。#1157 只按
# 字面修法加了上面那个。它比 `.out-backup/` 弱一点：happy path 里它被 rename 成 `out/<site>/`，
# 只有 `restore` 中途死掉才留在盘上 —— 但"弱一点"不是不会发生，而漏在盘上的后果跟上面那条逐字相同。
#
# 🔴 **复算这两条要带尾斜杠，或者先把目录建出来**（#1158 r2，QA1 抓的）：这两行是 `名字/` 形态，
#    只匹配**目录**；而 `git check-ignore <路径>` 在那个路径不存在时按**非目录**判 ⟹ 不带斜杠时
#    改前改后**都是 rc=1**，照抄会读成"这条修法没生效"。两种写法都对：
#      git check-ignore -v templates/nextjs/.out-temp/          # rc=0
#      mkdir -p templates/nextjs/.out-temp && git check-ignore -v templates/nextjs/.out-temp
.out-temp/

# generated at runtime
config/
sites/
src/lib/config-data.ts
# #1002 — sync-config.js writes these on every build (the 'skin' and this site's tweaks).
# The durable copies live in the SITE repo as site/theme.css / site/custom.css.
public/theme.css
public/custom.css

# #1215（来源 #1182 条 20）—— `scripts/theme-pipeline/run.js` 的收工纸条：开跑前落下、收工时撕掉，
# 下一轮拿它把上一轮没走完留下的脏样例站放回去。它必须活过重启，所以不能落 /tmp（这台机器开机清空
# 它），于是落在这里 —— 同 `.out-backup/` 的理由：不忽略就长得像「有人漏 stage 了一个交付物」。
.theme-pipeline-restore.json
# #1234 打磨批次 #26 条 13 —— 那张纸条现在是「先写 .<pid>.tmp 再原子 rename」写下去的（这样写到一半
# 被 kill 也不会在目标路径上留下半截 JSON）。代价是崩溃那一次会在盘上留一个 .tmp —— 同一个理由，
# 不忽略它就长得像漏 stage 的交付物。
.theme-pipeline-restore.json.*.tmp

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# playwright
test-results/
